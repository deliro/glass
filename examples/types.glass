pub enum Phase {
    Lobby
    Playing(Int)
    Victory(Int)
}

pub struct Model {
    phase: Int,
    wave: Int,
    score: Int,
}

fn test_construct() -> Model {
    Model { phase: 0, wave: 1, score: 100 }
}
